# Trident v0.12.0 scaffold for C-Z1 (Pyth Lazer signature integrity).
# Iteration budget matches the atlas CI per-cell budget
# (docs/design_notes.md). Runtime execution deferred consistent with
# the atlas's existing Trident deferred-runtime pattern (see the
# `collateral_mint_ref` cell). This scaffold is schema-checked in CI.

[fuzz]
fuzz_iterations = 1000
seed = 0
log_level = "info"

[programs]
pyth_lazer_solana_contract = "../../clean/pyth-lazer-solana-contract"
