# Trident v0.12.0 harness crate  - generated by cf-invariants-anchor-emit.
# Additive-only: sits under `trident-tests/collateral_mint_ref/`, no touching
# of the program crate.
[package]
name = "collateral_mint_ref_trident"
version = "0.1.0"
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
trident-fuzz = "=0.12.0"
anchor-lang = "=1.0.1"
anchor-spl = "=1.0.1"
collateral_mint_ref = { path = "../../programs/collateral_mint_ref", features = [] }
